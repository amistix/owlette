<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_edit_tunnels"
        android:name="org.amistix.owlette.ui.EditTunnelsFragment"
        android:label="Edit Tunnels"
        tools:layout="@layout/fragment_edit_tunnels" 
        android:launchMode="singleTop"/>

    <fragment
        android:id="@+id/nav_channel"
        android:name="org.amistix.owlette.ui.ChannelFragment"
        android:label="Channel"
        tools:layout="@layout/fragment_channel" />

    <fragment
        android:id="@+id/nav_home"
        android:name="org.amistix.owlette.ui.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="org.amistix.owlette.ui.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />

    <fragment
        android:id="@+id/nav_qrcode"
        android:name="org.amistix.owlette.ui.QrCodeFragment"
        android:label="Qr-code"
        tools:layout="@layout/fragment_qrcode" />

    <fragment
        android:id="@+id/nav_channel"
        android:name="org.amistix.owlette.ui.ChannelFragment"
        android:label="Channel"
        tools:layout="@layout/fragment_channel" />
</navigation>
